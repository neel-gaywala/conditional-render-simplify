{
  "name": "conditional-render-simplify",
  "version": "0.1.3",
  "private": false,
  "main": "dist/index.js",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/neel-gaywala/conditional-render-simplify.git"
  },
  "homepage": "https://github.com/neel-gaywala/conditional-render-simplify#readme",
  "dependencies": {
    "prop-types": "^15.8.1",
    "react": "^18.2.0"
  },
  "scripts": {
    "build": "babel src -d dist",
    "test": "echo \"Error: no test specified yet\" && exit 1",
    "prepublish": "npm run build && git tag v$(node -p \"require('./package.json').version\")",
    "postpublish": "git push origin --tags"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "@babel/cli": "^7.18.6",
    "@babel/preset-react": "^7.18.6"
  }
}
